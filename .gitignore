__pycache__/
*.pyc
*.egg-info/
.venv/
experiments/
checkpoints/
.pytest_cache/
.DS_Store
repo_summary/
# Intermediate GIF frames (assembled into the final .gif by plot_mcts_tree_gif.py)
**/_frames_*/
# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
# Experiment console logs are results, not build artifacts -- keep them despite *.log
!Claude-experiments/**/*.log
# Memoised lmfit R^2 lookups: ~10 MB per problem seed, rebuilt on demand by
# Claude-scripts/01-state-value-gap.py. Derived data, so kept out of history.
Claude-experiments/**/cache/
