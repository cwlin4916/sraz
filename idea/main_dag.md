<!-- The index: one line per node, a projection of the node.md files and never
     the storage place of any fact. Line schema:

     - [<id>](nodes/<path>/node.md) — <short title> · <status> · <verify> · depends-on: <ids or —>

     where <path> is the id split at each `-`, successive prefixes joined with `/`
     (I1-2 → nodes/I1/I1-2). Statuses: stated | attempting | verified | refuted | parked.
     `ready`/`blocked` are derived from dependencies, never written here.

     For now this file is maintained by hand, faithfully to the invariant above;
     the BEGIN/END markers reserve the block a future generator will own. -->

# Idea DAG — index

**Tally:** 0 nodes · 0 verified · 0 attempting · 0 stated

<!-- BEGIN INDEX -->

*(no nodes yet — copy `nodes/_template.md` to `nodes/I1/node.md` to start)*

<!-- END INDEX -->
