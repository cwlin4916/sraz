<!-- Node template. Copy to nodes/<id>/node.md and fill in.

     The node's directory is its id split at each `-`, successive prefixes joined
     with `/`: I1-2-1 lives at nodes/I1/I1-2/I1-2-1/. Links below are written for a
     top-level idea; each extra `-` in the id adds one `../`. Work them out from the
     node's own depth — do not copy them from a neighbour.

     report.md is written only once the idea is attempted: ONE PAGE, at most 900
     words, restating the statement verbatim and then settling it. Math in standard
     KaTeX only. -->

# I? — short title

[DAG index](../main_dag.md) · [conventions](../README.md)

- **created:** YYYY-MM-DD
- **status:** stated
- **verify:** math | empirical
- **depends-on:** — *(or links: [I?](../I?/node.md))*
- **required-by:** —
- **report:** — *(link report.md once written)*

## Statement

One paragraph: the idea, stated precisely enough to be verified or refuted. For a concept rather than a claim, state what adopting it would let you do, as a checkable assertion.

## Verification target

What settles this node. `math`: the statement to prove, and from what. `empirical`: the experiment — what is run, on what, and the success criterion fixed *before* running it.

## Remark

Context, provenance, or a contested reading. Delete the heading if empty.
